# Copie para .env e ajuste. O app lê direto do ambiente (sem lib de dotenv):
#   set -a && source .env && set +a && go run ./cmd/api
DATABASE_URL=postgres://studycentral:studycentral@localhost:5435/studycentral?sslmode=disable
PORT=8080
APP_ENV=development
# Origem do PWA em Next.js
CORS_ORIGIN=http://localhost:3000
